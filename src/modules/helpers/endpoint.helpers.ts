
const host = `https://my-mongodb-617s.onrender.com/mongodb/andarilo/agenda`;

const endpoint = {
    add: `${host}/create`,
    edit: `${host}/edit`,
    view: `${host}/view`,
    delete: `${host}/delete`,
}

export default endpoint;