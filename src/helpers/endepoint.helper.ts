
const host = 'https://my-mongodb-617s.onrender.com/prisma/imc';
const endpointHelpers = {
    view: `${host}/view`,
    add: `${host}/add`,
    delete: `${host}/delete`,
};

export default endpointHelpers;